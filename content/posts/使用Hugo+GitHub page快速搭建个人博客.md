+++
date = '2025-11-09T17:22:55+08:00'
draft = false
title = '使用Hugo+GitHub Page快速搭建个人博客'
+++

在本教程中，你将学会如何利用 Hugo（一个高效的静态网站生成器）和 GitHub Pages 免费快速搭建属于你自己的个人博客。全流程覆盖从环境安装、写作、主题美化，到部署上线，适合对前端不熟悉、但愿意动手实践的你！

---

## 为什么选择 Hugo + GitHub Pages？

- **Markdown 写作体验**：用最简洁的 Markdown 语法写文章，专注内容本身。
- **极速编译 & 性能**：Hugo 生成网站几乎是秒级，生成的全是静态页面，加载飞快。
- **免费托管**：GitHub Pages 提供免费高速 CDN，无需额外服务器。
- **极客成长路径**：包含 Git 版本管理、CI/CD 自动化部署，堪称“小型全栈实践”。
- **安全性强**：纯静态页面，无数据库、PHP 后台减少潜在攻击面。

---

## 搭建步骤

### 1. 安装 Hugo

- **macOS:**  
  `brew install hugo`

- **Windows:**  
  推荐用 `choco install hugo-extended`，或到 [Hugo Releases](https://github.com/gohugoio/hugo/releases) 下载适合的 `.exe` 二进制包。

### 2. 新建博客项目

在任意文件夹下执行：

```bash
hugo new site my-blog
cd my-blog
```

结构简介：
- `content/`：所有文章（Markdown 格式）都放这里。
- `themes/`：博客主题目录。
- `static/`：静态资源如图片、CSS。
- `config.toml`：全局配置文件。

### 3. 安装和配置主题

1. 浏览并挑选心仪主题：[Hugo 主题站](https://themes.gohugo.io/)（推荐 `ananke`、`even`、`stack` 等）。

2. 用 Git 添加主题为子模块（以 ananke 为例）：

   ```bash
   git init
   git submodule add https://github.com/theNewDynamic/gohugo-theme-ananke.git themes/ananke
   ```

3. 编辑 `config.toml`，设置站点信息和主题：

   ```toml
   baseURL = "https://你的github用户名.github.io/"
   languageCode = "zh-CN"
   title = "我的个人博客"
   theme = "ananke"
   ```

> 💡 每个主题还有更多自定义项，请查阅对应主题的 GitHub 文档。

### 4. 写第一篇博客文章

运行：

```bash
hugo new posts/my-first-post.md
```

然后编辑 `content/posts/my-first-post.md`，内容模板通常如下：

```markdown
---
title: "My First Post"
date: 2025-11-08T19:30:00+08:00
draft: true  # 草稿，正式发布记得改成 false
---

正文内容从这里开始……
```

记得将 `draft: true` 改为 `false`，文章才会被生成上线！

### 5. 本地预览

实时预览你的网站，命令如下：

```bash
hugo server
```

浏览器访问 [http://localhost:1313/](http://localhost:1313/)，修改文档会自动热更新，非常方便。

### 6. 部署到 GitHub Pages

最后把你的博客上线，供全球访问！

**步骤：**

1. **新建仓库**  
   登录 GitHub，创建一个**公开**仓库，命名为 `你的GitHub用户名.github.io`。  
      * 在 GitHub 上创建一个**公开** (Public) 仓库。
      * **仓库名必须是：`你的GitHub用户名.github.io`**
      * 例如，如果你的用户名是 `coder-zhang`，仓库名就是 `coder-zhang.github.io`。

2.  **生成静态文件：**

      * 在本地 `my-blog` 目录下运行 `hugo` 命令：
        ```bash
        hugo
        ```
      * Hugo 会“编译”你的网站，默认把所有生成的 HTML/CSS/JS 文件都放进一个叫 `public` 的文件夹里。

3.  **推送 `public` 文件夹的内容到仓库：**

      * 进入 `public` 文件夹，把它初始化为一个 Git 仓库，并推送到你刚才创建的 `username.github.io` 仓库的 `main` 分支。
        ```bash
        cd public

        git init
        git add .
        git commit -m "Initial commit"

        # 关联到你的远程仓库 (注意替换成你自己的)
        git remote add origin https://github.com/coder-zhang/coder-zhang.github.io.git
        git branch -M main

        # 推送！
        git push -u origin main
        ```

4.  **完成！**

      * 等待几分钟，访问 `https://coder-zhang.github.io`，你的博客就已经上线了！

-----

